import type { NextConfig } from "next";

// next.config.js
const nextConfig = {
  images: {
    domains: ['cdn.shopify.com', 'picsum.photos', 'lh3.googleusercontent.com'],
  },
};

export default nextConfig;
